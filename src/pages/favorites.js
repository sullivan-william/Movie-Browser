import React from 'react';

const Favorites = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Favorites</h1>
    </div>
  );
};

export default Favorites;